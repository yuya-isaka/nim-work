
echo 1.0e9 # 10億

var x, y: int
x = 3
y = 4
echo "x ", x
echo "y ", y

var g, z = 3
echo g
echo z

var
  a, b, c: string

const mina = "kawaii" # コンパイル時に強制的に計算されデータ領域に代入
let isaka = "kawaii" # 再代入できない

let input = readLine(stdin)